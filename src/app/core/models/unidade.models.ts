export interface UnidadeRequest {
  nome: string;
  ies: string;
  responsavel: string;
}

export interface UnidadeResponse {
  id: string;
  nome: string;
  ies: string;
  responsavel: string;
  status: string;
}
